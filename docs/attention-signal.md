# 三种 query-agnostic 打分信号的注意力热点（实测）

> 脚本：`bench/attn_signal_heatmap.py`｜图：`docs/figs/attn-signal/`｜2026-09
> 设置：Qwen2.5-1.5B，**最后一层**（layer 27），context 484 token，needle（`K7XQ21`）
> 放在第 **237** 位。三种信号都归一化成"每个位置的重要性"。

## 三种信号是什么

| 信号 | query 从哪来 | 语义 |
|---|---|---|
| **LM** | 模型自己的 Q（prefill 的因果注意力） | 语言建模"平时看谁" |
| **KVzip** | "请复述上文"探针（带 cache 跑一遍，读注意力） | 重建"需要谁" |
| **K-as-Q** | K 自己（`softmax(K Kᵀ/√d)`） | 自相似"谁冗余/中心" |

## 结果

| 信号 | top-10 位置 | needle rank | needle 占比 | 归一化熵 |
|---|---|---|---|---|
| **LM** | `0,4,6,8,11,12,20,23,35,47` | **98/484** | 0.195% | 0.845 |
| **KVzip** | `0,2,5,11,`**`229,232,233,234,237,242`** | **6/484** | **1.910%** | 0.873 |
| **K-as-Q** | `297,309,313,319,320,321,325,369,381,405` | **431/484** | 0.034% | 0.907 |
| **‖V‖**（value 范数） | — | 171/484 | 0.30% | 0.959 |
| **KVzip·‖V‖** | （同 KVzip） | **6/484** | — | — |

（needle 在第 237 位）

### Spearman 相关矩阵

| | lm | kvzip | kasq | ‖V‖ | kvzip·‖V‖ |
|---|---|---|---|---|---|
| **lm** | +1.00 | +0.62 | −0.45 | −0.35 | +0.27 |
| **kvzip** | +0.62 | +1.00 | **−0.53** | −0.15 | +0.74 |
| **kasq** | −0.45 | −0.53 | +1.00 | +0.22 | −0.27 |
| **‖V‖** | −0.35 | −0.15 | +0.22 | +1.00 | +0.44 |

### 集中度（top10% 覆盖）

lm 57.7% ｜ kvzip 54.9% ｜ kasq 42.9% ｜ **‖V‖ 仅 15.2%（几乎是平的）**

## 结论

1. **复述 vs baseline(LM)：+0.62 正相关，但 top-k 完全不同。**
   相关只来自"两者都含 sink"；LM 的 top-10 **全在开头 47 位**，复述的 top-10 **含 needle 一簇**。

2. **复述 vs K-as-Q：−0.53 负相关**——两个信号在"选谁"上**系统性相反**。
   自相似奖励冗余/中心，复述奖励信息/离群，方向反了。

3. **‖V‖（value 范数）几乎是平的**（top10% 只占 15.2%，needle rank 171），
   和谁都不太相关 → **单独不能当重要性信号**。KVzip 只把它当**乘子**：
   `kvzip·‖V‖` 与 `kvzip` 相关 +0.74、needle rank 仍是 6，**排序基本不变**（只微调）。
   → 这解释了为什么 KVzip 把它放在最后一步做 rescale，而不是打分本身。

4. **三者的"总量分布"看起来接近（熵 0.85–0.96），差别全在 top-k 打在哪。**

## 结构：热力图里到底长什么样（数出来的，不是看出来的）

**LM 注意力的 argmax 偏移分布**（key − query）：

| 偏移 | 占比 | 含义 |
|---|---|---|
| **0** | 13.2% | 看自己 |
| −1 | 3.3% | 前一个 token（局部） |
| **−11 / −23 / −35 / −47** | 各 1.2–2.7% | **周期 ≈12** —— 正好是填充句的长度：模型在"上一句同位置"上打转 |
| 其余 | 摊薄 | **几乎没有语义峰值** |

→ LM 热力图 = **sink 竖条 + 自指/前一位 + 周期性重复**，跟"信息在哪"无关。

**KVzip 探针的 argmax 偏移**：最强只有 `+1`（1.7%，类 induction head），其余是散开的大负偏移。
→ 所以**"逐 token 复制原文"的对角线并不存在**（copy rate 0.4%）——我原先的对角线假设被否掉了。

## 关键消融：优势有多少来自"max 聚合"？

| 信号 | sink64 | top10% | **needle rank** |
|---|---|---|---|
| LM（**sum** 聚合） | 51.6% | 57.7% | **98** |
| **LM（max 聚合）** | 26.4% | 24.3% | **16** |
| KVzip（复述 + max） | 37.1% | 54.9% | **6** |

**只把 `sum` 换成 `max`，needle 就从 rank 98 → 16**，且 sink 占比从 51.6% → 26.4%、
集中度从 57.7% → 24.3%。**复述探针再把它从 16 → 6。**

⇒ **KVzip 的优势是两个因素叠加：**
1. **聚合方式（sum → max）**：`sum` 会把"很多 query 各看一点"的 sink 累积成高分；
   `max` 捕捉"**有 query 强烈需要它**"——这才是信息语义。**这是更大的那一半。**
2. **探针（LM query → 复述指令 query）**：16 → 6，是次要但真实的一半。

相关性也印证：`kvzip` 与 `lm_sum` 相关 **+0.62**，但与 `lm_max` 只有 **+0.33** ——
说明探针产生的信号和"LM 注意力取 max"确实**不是一回事**。

## 对我们的意义

- 我们的 `PE_SCORE_MODE=context`（采样 prompt query + max）≈ **LM 那一行**，所以它在
  needle 类任务上会漏掉信息 token（needle rank 98）。
- 这解释了为什么 LongBench F1 上 context 够用（答案不依赖单点），但**在 retrieval/needle
  类高压缩比任务上，KVzip 式重建应该明显更好**——这一项我们还没测（TODO）。
- `expected`（ExpectedAttention）≈ 用 query 分布估计，也在 LM 这一族里，所以它没优势。

## 备注

- 图是用 matplotlib 生成的（`docs/figs/attn-signal/{signals_curve,map_lm,map_kvzip,map_kasq}.png`）。
  本轮 `look_at`（图像分析）工具超时，**图未经人眼复核**；上表数字是从原始张量直接统计的，
  作为证据更可靠。
- 只在**最后一层**测；换层/换位置结论可能变（我们之前的 `attn_layer_analysis` 显示层间差异大）。
